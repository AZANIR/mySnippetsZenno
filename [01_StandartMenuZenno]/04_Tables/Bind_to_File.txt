//Создаём объект класса IZennoTable, привязываемся к списку уровня проекта
IZennoTable tblTest = project.Tables["Таблица 1"];
tblTest.Clear(); //очищаем таблицу

string strFilePath = project.Directory + @"\Сохранённая_таблица.csv"; //путь к файлу, к которому будем привязываться

//Предварительно создаём пустой файл, к которому будет осущетсвляться привязка (раскомментируйте строку в случае необходимости)
//File.WriteAllText(strFilePath, String.Empty);

//IZennoTable.Bind - привязываем таблицу к файлу
tblTest.Bind(strFilePath);

project.SendInfoToLog("Таблица перепривязана. Первая строка таблицы: " + String.Join("|", tblTest.GetRow(0)));

//Примечания:
//1. Перед запуском этого сниппета выполните предыдущий (сохранить в файл) для создания файла Сохранённая_таблица.csv
//2. Привязку к xlsx-файлу можно использовать как "сохранение", если файл до этого не существовал.