// Прокрутка колесиком мыши
//Прокрутка по горизонтали, в пикселях
int deltaX = 0;
//Прокрутка по вертикали, в пикселях
int deltaY = 1000;

//Делаем прокрутку на нужное количество пикселей
instance.ActiveTab.FullEmulationMouseWheel(0, 1000);

// Примечание
//Для прокрутки вниз (по вертикали) или вправо (по горизонтали)
//нужно использовать положительные значения.
//Для прокрутки вверх (по вертикали) или влево (по горизонтали)
//нужно использовать отрицательные значения (со знаком "-").